// Configuración de caché para las páginas de eventos individuales
export const dynamic = 'force-dynamic';
export const revalidate = 0;

export default function EventLayout({ children }) {
  return children;
}
